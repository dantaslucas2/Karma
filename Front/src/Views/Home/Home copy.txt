import React, { Component} from 'react';
import Redirect from '../../Components/Redirect/Redirect';
import Slider from '../../Components/SlideHome/SlideHome';
import imagesHome from '../../Assets/Home/ImagesHome.json'

class Home extends Component<any,any>{

    constructor(props: []) {
        super(props);
        this.state = {
            Slide:imagesHome,
            visibleItemsNumber: 3
        };
    }

    renderImage(slide: IPropImageHome){
        return(
            <img
                id={slide.id}
                src={slide.url}
                alt="nature landscape"
                className="image"
            />
        )
    }

    render(){

        console.log(this.state.Slide)
        return (
            <div>
                <div className="App">

                    <Slider slides={this.state.Slide} visibleItemsNumber={this.state.visibleItemsNumber}>
                        {(slide: IPropImageHome) => (
                            <img
                                id={slide.id}
                                src={URL.createObjectURL('../../Assets/images/logo.png')}
                                alt="nature landscape"
                                className="image"
                            />
                        )}              
                    </Slider>
                </div>
                <div className="Buttons">
                <img id="2" src={require('../../Assets/images/logo.png')} alt="nature landscape" className="image"/>
                    <Redirect label="Login" url="/Login"/>
                    <Redirect label="Cadastre-se" url="/cadastre-se"/>
                </div>
            </div>
    );
  }
}


export default Home;